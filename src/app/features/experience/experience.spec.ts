import '@angular/compiler';
import { describe, expect, it } from 'vitest';
import { ExperienceComponent } from './experience';

describe('ExperienceComponent', () => {
  it('should be defined', () => {
    expect(typeof ExperienceComponent).toBe('function');
  });
});
