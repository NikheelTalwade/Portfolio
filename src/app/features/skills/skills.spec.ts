import '@angular/compiler';
import { describe, expect, it } from 'vitest';
import { SkillsComponent } from './skills';

describe('SkillsComponent', () => {
  it('should be defined', () => {
    expect(typeof SkillsComponent).toBe('function');
  });
});
