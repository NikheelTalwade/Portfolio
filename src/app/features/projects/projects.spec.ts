import '@angular/compiler';
import { describe, expect, it } from 'vitest';
import { ProjectsComponent } from './projects';

describe('ProjectsComponent', () => {
  it('should be defined', () => {
    expect(typeof ProjectsComponent).toBe('function');
  });
});
